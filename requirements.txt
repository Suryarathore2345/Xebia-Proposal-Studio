# Xebia Proposal Studio - Python Dependencies

# Web server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.0.0

# LLM (Claude Sonnet 5)
anthropic>=0.40.0

# LLM (Gemini) - testing toggle only, see src/llm/gemini_client.py
google-genai>=0.3.0

# Document processing
python-pptx>=1.0.0
python-docx>=1.1.0
Pillow>=10.0.0
lxml>=5.0.0

# Icon library build (scripts/build_icon_library.py) - rasterizes
# downloaded SVG icons to PNG. cairosvg needs the native libcairo
# library, which isn't installed on some machines (e.g. plain Windows);
# resvg-py is a self-contained fallback (Rust binary wheel, no system
# dependency) the script uses automatically when cairosvg can't load.
cairosvg>=2.7.0
resvg-py>=0.5.0

# Embeddings (local, zero-cost)
sentence-transformers>=3.0.0

# Database (install when PostgreSQL is ready)
# psycopg2-binary>=2.9.9
# pgvector>=0.3.0
# alembic>=1.13.0

# Data processing
pyyaml>=6.0
numpy>=1.26.0

# Utilities
tqdm>=4.66.0
rich>=13.0.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
